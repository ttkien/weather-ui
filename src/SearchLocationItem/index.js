import React from 'react';
import { Text, View } from 'react-native';

var SearchLocationItem = (props) => {
        return (
            <View>
                <Text>{props.name}</Text>
            </View>
        )
}

export default SearchLocationItem